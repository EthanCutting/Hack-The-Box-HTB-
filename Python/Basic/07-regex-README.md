# 🔍 Regular Expressions (Regex)

Regex is used for **pattern matching in text**.

---

## Basic Example

```python
import re
re.search(r"error", log_line)
```

---

## Common Uses
- Log parsing
- Input validation
- IOC detection

---

